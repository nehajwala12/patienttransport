# Tiger / EMED — NEPTS Sector Intelligence Packet (creditswan.ai) — Rev 6

Static, single-file site. `index.html` is self-contained (inline CSS/JS/SVG; Google Fonts loaded at runtime).

Deploy on Vercel via GitHub: add `index.html` and `README.md` at the repository root → Vercel **Add New → Project → Import** → framework preset **Other**, no build command, root output. Sections are addressable by hash (e.g. `/#decision`, `/#sources`).

Sources: S1–S70 public record; D1–D12 third-party and source-system data (registered in the Sources tab; convention in Appendix §F). Robots: noindex.
